---
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>360ENGAGE</title>
    <meta name="description" content="360ENGAGE is currently unavailable." />
    <style>
      :root { color-scheme: dark; }
      * { box-sizing: border-box; }
      body {
        align-items: center;
        background: #0b1220;
        color: #f8fafc;
        display: flex;
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        justify-content: center;
        margin: 0;
        min-height: 100vh;
        padding: 2rem;
      }
      main { max-width: 38rem; text-align: center; }
      .wordmark { font-size: 0.875rem; font-weight: 800; letter-spacing: 0.18em; margin: 0 0 2.5rem; }
      h1 { font-size: clamp(2.1rem, 7vw, 4.25rem); letter-spacing: -0.045em; line-height: 1.04; margin: 0; }
      p { color: #cbd5e1; font-size: clamp(1rem, 2.2vw, 1.2rem); line-height: 1.65; margin: 1.5rem auto 0; max-width: 32rem; }
    </style>
  </head>
  <body>
    <main>
      <p class="wordmark">360ENGAGE</p>
      <h1>Currently unavailable.</h1>
    </main>
  </body>
</html>
